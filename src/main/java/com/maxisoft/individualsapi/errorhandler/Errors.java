package com.maxisoft.individualsapi.errorhandler;

public enum Errors {
    UNKNOWN,
    INTERNAL_ERROR,
    UNAUTHORIZED,
    INVALID_CREDENTIALS,
    INVALID_TOKEN,
    JWT_EXPIRED,
    INVALID_JWT
}
